# Optional: Groq free-tier API key (get at https://console.groq.com)
# If not set, the app uses Ollama (local) for the LLM.
GROQ_API_KEY=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

# Optional: max upload size in bytes (default 20MB)
# MAX_FILE_SIZE=20971520

# Optional: CORS is set in code; override via ALLOWED_ORIGINS if needed
# ALLOWED_ORIGINS=http://localhost:5173
